[workspace]
members = [
    "activity-sleepy",
    "workflow-tutorial",
]
resolver = "2"

[workspace.package]
version = "0.1.0"
authors = ["Project Developers"]
license = "MIT"
repository = "TBD"
edition = "2024"
rust-version = "1.90.0"

[workspace.dependencies]
wit-bindgen = "0.48.0"

[profile.release]
opt-level = "s"
lto = true
overflow-checks = true

[profile.workflow]
inherits = "release"
opt-level = "s"
debug = "line-tables-only"
